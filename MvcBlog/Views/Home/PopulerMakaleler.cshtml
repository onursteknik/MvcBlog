@model IEnumerable<MvcBlog.Models.Makale>

@{
    Layout = null;
}

@*<h4>Popüler Makaleler</h4>*@

@*<div class="row">
        <div class="col-lg-12">
            <ul>
                @foreach (var item in Model)
                {
                    <li><a href="/Home/MakaleDetay/@item.MakaleID">@item.Baslik</a> (@item.Okunma)</li>
                }

            </ul>

        </div>

    </div>*@

    <div class="panel-heading"><h4 style="text-align:center">Popüler Makaleler</h4></div>
    <div class="panel-body">
        <div class="list-group" style="text-align:center">
            @foreach (var item in Model)
            {
            <a href="/Home/MakaleDetay/@item.MakaleID" class="list-group-item">@item.Baslik (@item.Okunma)</a>
            }
        </div>
    </div>